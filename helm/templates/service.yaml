apiVersion: v1
kind: Service
metadata:
  name: {{ include "tompa.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tompa.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "tompa.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
