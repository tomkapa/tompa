apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tompa.configMapName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tompa.labels" . | nindent 4 }}
data:
  RUST_LOG: {{ .Values.config.rustLog | quote }}
  PORT: {{ .Values.config.port | quote }}
